unit UBridge;

interface

uses
  Classes, SysUtils;

type
  // Implementor
  IDrawingAPI = interface
    procedure DrawShape(x, y : integer);
  end;

  // Abstraction
  TShape = class
  private
    FDrawingAPI : IDrawingAPI;
  public
    procedure Resize(APercent : double); abstract;
  end;

implementation

end.
